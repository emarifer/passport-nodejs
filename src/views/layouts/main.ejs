<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login with Passport - NodeJS</title>
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="css/custom.css">
    <link rel="shortcut icon" href="img/favicon.png" type="image/x-icon">
</head>

<body class="bg-info">
    <nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand text-light">Login Passport NodeJS</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="<%= prefixApp %>/profile">Profile</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="<%= prefixApp %>/logout">Logout</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="<%= prefixApp %>/signin">SignIn</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="<%= prefixApp %>/signup">SignUp</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <% if (signupMessage.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show">
            <strong><%= signupMessage %></strong>
            <div class="float-right" data-bs-dismiss="alert">X</div>
        </div>
        <% } %>
        <% if (signinMessage.length > 0) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <strong><%= signinMessage %></strong>
            <div class="float-right" data-bs-dismiss="alert">X</div>
        </div>
        <% } %>
        <%- body %>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
</body>

</html>